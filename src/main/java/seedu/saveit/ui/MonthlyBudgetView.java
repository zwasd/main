package seedu.saveit.ui;

import java.net.URL;

import javafx.scene.layout.Region;

/**
 * This is for budget view.
 */
public class MonthlyBudgetView extends UiPart<Region> {

    public MonthlyBudgetView(URL fxmlFileUrl) {
        super(fxmlFileUrl);
    }
}
